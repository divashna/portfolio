// Smooth scrolling
function smoothScroll() {
    document.documentElement.style.scrollBehavior = "smooth";
};

if (document.getElementsByClassName("mainHeading").length < 100 ) {
    document.getElementById("headline")
};
